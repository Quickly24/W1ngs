# W1ngs
CLI "app"

Global functions, classes, structs ~> PascalCase
Variables ~> snake_case 
Local function, classes, structs ~> camelCase

TODO:
.show list of TODO items with some affix
Writing notes
.file for substituting strings
 (greek letters, special characters, colors
 "\red{string}" -> string (in red)
 "\delta" -> delta_character)
Calendar
Planner
Some art visual overhaul
Counter